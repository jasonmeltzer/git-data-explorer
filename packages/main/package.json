{
  "name": "@git-data-explorer/main",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@git-data-explorer/shared": "*"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "tsx watch server/index.ts",
    "dev:client": "vite",
    "seed": "tsx scripts/seed.ts",
    "dev:seed": "DB_PATH=./data/seed.db concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate"
  }
}
