---
phase: 03-data-collection
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified: []
autonomous: false
requirements: [COLL-06, COLL-07, COLL-09]

must_haves:
  truths:
    - "User can trigger collection and see live progress in the browser"
    - "All Phase 3 success criteria are met end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete data collection flow — from clicking "Sync now" through live progress display, rate-limit handling, and data completeness indicators.

Purpose: Verify the end-to-end flow works in a real browser with real GitHub API calls before marking Phase 3 complete.

Output: Confirmation that all Phase 3 success criteria pass, or a list of issues to fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-data-collection/3-CONTEXT.md
@.planning/phases/03-data-collection/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify end-to-end data collection flow</name>
  <files>none</files>
  <read_first>
    - .planning/phases/03-data-collection/03-03-SUMMARY.md (what was built in previous plan)
    - .planning/phases/03-data-collection/3-CONTEXT.md (user decisions to verify against)
  </read_first>
  <action>
    Start the dev server with `npm run dev`. Open http://localhost:5173 in the browser. Walk through the verification steps below to confirm the complete data collection flow works end-to-end.

    If any step fails, document the exact failure for the user to review.

    Verification steps:
    1. Navigate to Settings — confirm "Include bot accounts" toggle is visible and defaults to OFF
    2. Navigate to Repos page — confirm two tabs exist: "Repos" and "Collection"
    3. Ensure at least one repo is tracked (if not, add a small public repo)
    4. Click the "Collection" tab
    5. Click "Sync now" button
    6. Confirm button shows loading state ("Syncing..."); per-repo status list appears with "Collecting" badge and live item counts incrementing
    7. Confirm items fetched counts update in real-time (e.g., "42 commits, 12 PRs" incrementing)
    8. Wait for at least one repo to complete — confirm it shows "Up to date" with a "last synced" timestamp
    9. Switch to "Repos" tab — confirm incomplete repos show collection badges ("Collecting...")
    10. Switch back to "Collection" tab — confirm "X bot accounts excluded" text appears below the list if bots were detected
    11. If rate limit is reached: confirm message "Rate limited -- collection will resume after {time} automatically." appears
    12. Test "Stop all" during active collection: confirm collection pauses and states are preserved
    13. Close and reopen the app — confirm resume banner appears if collection was incomplete
  </action>
  <verify>
    <automated>npm run dev -- --port 5173 &amp; sleep 3 &amp;&amp; curl -s http://localhost:5173 | head -5 &amp;&amp; echo "Dev server running"</automated>
  </verify>
  <acceptance_criteria>
    - Dev server starts without errors on `npm run dev`
    - Browser shows ReposPage with "Repos" and "Collection" tabs
    - "Sync now" button triggers collection and progress updates are visible
    - Settings page shows "Include bot accounts" toggle
  </acceptance_criteria>
  <done>User confirms the complete data collection flow works: collection triggers, progress displays live, rate-limit messaging appears when applicable, and bot toggle persists.</done>
</task>

</tasks>

<verification>
Human confirms visual and functional correctness of all collection UI elements.
</verification>

<success_criteria>
All Phase 3 success criteria from ROADMAP.md verified:
1. Collection fetches and stores PR and commit data; re-running does not re-fetch cached data
2. Rate-limit pause is detected and communicated clearly
3. Collection resumes from last cursor after restart
4. UI shows live progress with per-repo detail
5. Bot accounts are excluded from contributor analysis
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-collection/03-04-SUMMARY.md`
</output>
